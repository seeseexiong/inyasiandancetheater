$(document).ready(function(){

    // Initialization for Slider from materialize
    $('.slider').slider({
        full_width: true,
        height : 700, // default - height : 400
        interval: 4000, // default - interval: 6000
    });
       
    
    // Initialization for side nav bar from materialize
    $('.sidenav').sidenav();
});